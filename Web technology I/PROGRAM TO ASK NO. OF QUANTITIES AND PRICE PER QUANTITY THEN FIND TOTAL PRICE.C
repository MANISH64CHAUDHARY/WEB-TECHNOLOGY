/* program to ask no. of quantities and price for per quantity then find total price*/
#include <stdio.h>
#include <conio.h>
void main ()
{
    int n,p,tp;
    printf("Enter no. of quantities and price for per quantity:");
    scanf("%d%d", &n,&p);
    Total Price = n*p;
    printf("\nTotal price=%d",tp);
    getch();
}